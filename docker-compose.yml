version: "3.4"
networks:
  proxylampy:
    external: true
  net:
    driver: overlay
    attachable: true
services:
 www:
    image: node:17.4.0
    working_dir: /app
    command: >
      sh -c "npm install && npm run start"
    volumes:
      - ${PWD}/apps:/app
    networks:
      - proxylampy
      - net
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.nuxtjs.rule=HostRegexp(`nuxtjs.traefik.me`, `nuxtjs.{ip:.*}.traefik.me`)"
        - "traefik.http.services.nuxtjs.loadbalancer.server.port=4200"
